# Dynamic Gesture Recognizer (Python Part)
 
By using the hand-gesture-recognition-mediapipe-main -> Socket.py, TCP with Unity Project.

Socket.py receive the Index finger tip position and make queue (time stamp 8).
And then interpret the que and send the interpret result. 
